# Core Django
Django==5.2.6

# WSGI server for production
gunicorn==23.0.0

# Static files handling
whitenoise==6.10.0

# Django REST framework (APIs)
djangorestframework==3.15.2

# Filtering support for DRF
django-filter==24.3

# PostgreSQL adapter (needed if you’re using Render’s Postgres DB)
psycopg2-binary==2.9.9

# For handling environment variables (good practice on Render)
python-decouple==3.8

# If you use CORS (for frontend-backend communication)
django-cors-headers==4.4.0

# Authentication and JWT
djoser==2.3.1
djangorestframework-simplejwt==5.3.1
